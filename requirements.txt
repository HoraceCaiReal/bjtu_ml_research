# ---------- 深度学习框架（PyTorch CUDA 11.8 版本） ----------
torch==2.1.1+cu118
torchvision==0.16.1+cu118
--extra-index-url https://mirrors.aliyun.com/pytorch-wheels/cu118

# ---------- 传统机器学习 ----------
scikit-learn==1.4.2          # 决策树、SVM、K-Means
scikit-image==0.22.0         # 图像预处理（特征提取等）
xgboost==2.0.3               # 可选：梯度提升树对比

# ---------- 预训练模型库（可选扩展） ----------
timm==0.9.12                 # PyTorch Image Models

# ---------- 数据处理与可视化 ----------
numpy==1.26.4
pandas==2.1.4
matplotlib==3.8.4            # 微软雅黑字体已通过 Docker / VSCode 配置
seaborn==0.13.2
plotly==5.18.0               # 交互式图表
opencv-python==4.9.0.80      # 图像读取与基础处理
Pillow==10.2.0               # PIL 图像处理

# ---------- Jupyter 与交互式系统 ----------
jupyterlab==4.0.13
notebook==7.0.8              # 兼容传统 .ipynb 打开方式
ipywidgets==8.1.1            # 综合展示系统 GUI 核心
ipykernel==6.29.5            # VSCode 运行 Jupyter 所需
tqdm==4.66.6                 # 进度条

# ---------- 代码质量工具 ----------
ruff==0.15.11                # Python 代码格式化与检查（替代 Black）

# ---------- 工具库 ----------
PyYAML==6.0.3
scipy==1.11.4                # 科学计算（优化、统计等）
python-dotenv==1.0.0         # 读取 .env 环境变量
tensorboard==2.15.1          # 训练过程可视化（可选）
